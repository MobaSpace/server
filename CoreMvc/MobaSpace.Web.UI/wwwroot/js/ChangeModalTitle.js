/**
 * Update Modal Title
 * @param {any} idpatient
 */
function ChangeModalTitle(idpatient) {
    var Title = document.getElementById('DateNuits_' + idpatient).value;
    var date = document.getElementsByName("dateDebut").value;
    Title = date;
}